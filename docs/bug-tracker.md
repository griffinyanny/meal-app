# Bug Tracker — parked defects & risks

> **What this is.** The single home for every **known defect or risk we've chosen to park**
> rather than fix on the spot. A bug does not get "deferred to the backlog" — it gets an entry
> here, with a **repro**, a **severity**, a **when-to-address target**, and a **status we close out**.
> Nothing rots: every session end reviews the Open table, and closing an item moves it to the
> Resolved log with the date + how.
>
> **Distinct from the other docs:** `idea-backlog.md` = features/ideas; `open-questions.md` =
> unresolved *decisions*; **this file = reproducible things that are wrong or risky.** If you can
> write a repro for it, it belongs here.

**Severity:** 🔴 high (breaks a core flow / data / trust) · 🟠 med (degrades UX, has a workaround) ·
🟡 low (cosmetic / self-correcting).
**Status:** `open` · `in-progress` · `resolved` (→ moved to the Resolved log).

> **ID note (S48 merge):** the concurrent access-gate session allocated BUG-030/031 unaware the
> worktree had already spent them (BUG-030 stale capture spec, BUG-031 day-map). Its two findings were
> renumbered **BUG-042** (email-signup authorization assumption) and **BUG-043** (noindex outlives launch)
> at merge; the code comments in `next.config.ts` / `robots.txt` were repointed.

---

## Open

| ID | Sev | Summary | Repro | Found | Address by | Status |
|----|-----|---------|-------|-------|-----------|--------|
| BUG-023 | 🟡 | **`plan.modify` reports changed DAYS, not slot ids, so the change ring cannot be truly row-level at multi-meal density.** 1E.5 ledger §C: *"The meal row is the unit of change feedback, never the day container"* — the ring sits on the changed row's own 14px radius inside the day's 18px. The client resolves each changed date to the cookable rows on it (`plan-page-client.tsx`, `idsOnDates`), which at R1's one-dinner-per-day is **exactly** row-level. At the three-meal density the same ledger specifies, a whole-day change would ring dinner, lunch and breakfast together — feedback pointing at two rows the chef did not touch. | Not reachable in production: generation produces dinners only. Reachable the moment lunch/breakfast generation ships, or via a hand-seeded multi-meal day + `plan.modify`. | S43 (2026-07-27), building 1E.5 W1/W3 | **Before multi-meal generation ships** (V1.5+). Fix is server-side: return `changedSlotIds` alongside `changedDates` from `plan.modify`, and have the client prefer it. Cheap — the mutation already knows which slots it wrote. | `open` |
| BUG-043 | 🟠 | **`noindex` and `robots.txt` are unconditional and will silently outlive the closed beta.** `public/robots.txt` (`Disallow: /`) and the `X-Robots-Tag: noindex, nofollow` header in `next.config.ts` are deliberately NOT env-driven — `headers()` is evaluated at build time while the two access gates read env at request time, so wiring them together would let the two silently disagree. The cost of that choice is that going public requires a **code** change, unlike the gates themselves. Forgetting it is the classic way a launched product stays out of Google for weeks while everyone assumes SEO is "just slow." | Ship publicly with these in place → `curl -I https://<domain>/` still returns `X-Robots-Tag: noindex, nofollow`, and `/robots.txt` still says `Disallow: /`. | S43 (2026-07-28), by construction when the gate was built | **Launch day, as an explicit checklist item.** Delete `public/robots.txt` and the one header line. Both call sites carry a `LAUNCH-DAY ITEM` comment pointing back at this row. | `open` |
| BUG-044 | 🟡 | **`finishOnboarding`'s `dietaryFramework` is a bounded string where the persist path enforces an enum.** `user.updatePreferences` validates the framework against `dietaryFrameworkSchema` (8 values); `interviewStateSchema` accepts any `z.string().max(30)`. Not a text-injection path any more — BUG-013's fix made `synthesizeHeadlineMemory` DROP a framework it has no label for rather than echo it — but the two boundaries disagreeing about the same domain is how the echo got there in the first place. Narrowing it means the framework list has to live in one pure module that both the db schema and the client's `InterviewState` read, which is a four-file change and was out of scope for a ship-blocker. | `interviewStateSchema.safeParse({ ...state, dietaryFramework: 'anything' })` succeeds. | S51 (2026-07-30), fixing BUG-013 | 1F Workstream D, with the security review of the full surface. Move `DIETARY_FRAMEWORKS` into a pure module, build both the zod enum and `InterviewState['dietaryFramework']` from it, and type `DIET_LABEL` as `Record<DietaryFramework, string>` so a new framework cannot ship without a label. | `open` |
| BUG-017 | 🟡 | **The first-run gate is not synchronous with first paint.** `OnboardGuard` fires `ensureOnboarded` from an effect while the destination route renders and fires its own queries in parallel. On a genuinely first-ever login `ensureOnboarded` is the only thing that inserts the `household_members` row, and `protectedProcedure` throws FORBIDDEN without it — so the page's own queries can race the bootstrap and fail before the redirect lands. React Query's default retry papers over it incidentally, not by design. | Brand-new account, first ever load of `/plan`. Hard to reproduce reliably; read the code path. | S37 (2026-07-25), `/code-review` perf/UX lens | 1F hardening, unless it shows up in Griffin's or his wife's real first run — in which case it is the first thing to fix. | `open` |
| BUG-051 | 🟠 | **An item added offline is the S60 illusion one control over: it survives a cold launch and is then wiped by the first refetch.** `grocery.current` is on the persist allow-list and `addItem`'s `onMutate` patches a temp row straight into it, so the optimistic row is dehydrated to IndexedDB. But `addItem` is **not** in `OFFLINE_MUTATION_PATHS`, so the paused mutation that would save it is **not** persisted — deliberately, because it runs an AI categorize pass. The sequence is S60's own rejected case with `addItem` in `checkItem`'s role: add an item in a shop with no signal → iOS evicts the backgrounded PWA → relaunch → **the item is still there, because it was persisted** → signal returns → the refetch drops it. The user watched their work survive a relaunch and reasonably concluded it was saved. ⚠️ **The design frame for 1h says in as many words that "adding still works"**, so making the field provisional offline contradicts a locked artifact and is not the fix. | Offline (devtools or airplane mode) on `/groceries`: quick-add an item, confirm it renders, wait for the persist throttle (~1s), kill and relaunch the app, then go online. The row is present on relaunch and gone after the refetch. | S61 (2026-08-01), building 1F/C's offline clause | **1F Workstream D**, or a C follow-up — not the same session as the design artifacts. ⚠️ **The one-line version is the trap S60 named.** `checkItem` is idempotent so replay is safe; `addItem` is not, and a replay whose original POST landed but whose response was lost duplicates the row. Doing it properly means a client-supplied idempotency key: a column, a unique index and a migration — which also makes it the first non-additive-shaped migration and therefore D's `migrations.test.ts` guard's first real subject. The cheap alternative, if D would rather not spend that: strip un-replayable optimistic rows from the dehydrated snapshot, so the add is lost *with* the mutation it depended on rather than after it. That is "less feature", which Griffin has accepted before; the current state is "looks broken", which he has rejected by name. | `open` |
| BUG-053 | 🔴 | **The Groceries visual gate has been BLIND since S60, and S60's own 0/0 came from a run that predated the change it was grading.** `grocery-generating` and `grocery-error` both fail their `readyText` wait at 15s — the seeded state never renders — and `grocery-checked-gotit` then hangs indefinitely on a `.click()` that has no action timeout, because there are no rows to click. The run blows the 120s test budget and dies, so **no manifest is written and the whole Groceries surface goes ungraded.** ⚠️ **NOT caused by S61** — reproduced three times with S61's Groceries components reverted, and a fourth with the **entire source tree** checked out at `HEAD~1`. ⚠️ **The timeline is the finding:** the last successful Groceries capture is `A-groceries-2026-08-01T22-01-39-412Z` = **15:01 PDT on 2026-08-01**, and the offline half (service worker + React Query→IndexedDB persistence, PR #25, commit `572a16b`) merged at **18:42 PDT the same day**. So **no Groceries capture has ever run against a build containing the service worker**, and S60's handoff reported *"0 blockers / 0 high, 56/56 states ok"* from a pass run three and a half hours before the code it was meant to gate existed. *Same shape as S55's "present, correct, and UNRUN" and S54's stale build — a gate reporting on a tree that is not the one shipping.* | `npm run test:capture -- tests/e2e/capture/groceries.capture.ts` from the repo root. Only `grocery-ready-grouped.png` and `grocery-manual-mode.png` are written; the test times out at 120s with no `manifest.json`. The failure screenshot shows an **empty** list (`0 / 0`, "Your list is empty") where seeded content should be. | S61 (2026-08-02), running the `/visual-qa` pass owed for the offline clause | **Before Workstream C is called closed, and before any further Groceries UI work** — a design-system project whose visual gate cannot see one of its five surfaces is worse off than one with no gate, because the handoff still says 0/0. ⚠️ **Prime suspect is the service worker or the persisted query cache serving a stale/empty `grocery.current` across capture states** (the capture drives ONE page through every state, so a cache persisted in state N is restored into state N+1) — **but that is a hypothesis, not a measurement**, and this project's own rule is that the parked recommendation is the thing to distrust. **Measure first:** log per-state timing in `captureStates` and check whether the page is showing another state's list. Two apparatus fixes ride along regardless: `grocery-checked-gotit`'s unbounded `.click()` needs an explicit timeout so a missing row fails in seconds rather than eating the whole budget, and the runner should write its manifest even when the test times out — **the manifest is the only artifact that says WHICH state failed, and it is the one thing a timeout destroys.** | `open` |
| BUG-052 | 🟡 | **The maskable icon has a visible seam at its safe-zone boundary.** `generate-icons.mjs` builds it by creating a flat `#0F0B08` canvas and compositing an **80%-inset render of the whole artboard** onto it — so the outer 10% margin is bare floor while the pasted square carries §01's wash, and the boundary between them is a visible rectangle edge across the top. **The floor and the wash are background and should be full-bleed; only the MARK needs to respect the safe zone.** Pre-existing: the generator is unchanged since S59 and the placeholder icon had the identical structure, so S61's artwork did not introduce it. | Open `public/icons/icon-maskable-512.png`. A horizontal edge is visible across the upper ~10%, where the composited artboard begins. | S61 (2026-08-02), reviewing the regenerated rasters | **Whenever Android enters the picture — not in R1.** ⚠️ **Both R1 phones are iPhones (Griffin, S59) and iOS ignores `purpose: maskable` entirely**, so this asset is never rendered by anyone this release. The fix is to scale the orb + glow + toque to 80% about the centre while leaving floor and wash full-bleed, which means the icon SVG has to expose the mark as a transformable group (or the script has to build it the way `generate-splash.mjs` does). ⚠️ Do NOT "fix" it by dropping the wash from the maskable — the wash is what makes the icon sit in the same room as the app. | `open` |
| BUG-003 | 🟡 | **Cooked-harvest writes inside a `recipe.list` query.** `recipe.list` (a tRPC query / GET-shaped) runs `harvestCookedRecipes` — a grouped select + per-recipe UPDATEs — on every load and every favorite invalidation. Idempotent & documented, but write-work on a read path that React Query may refetch/retry. | N/A (design smell, not a user-visible fault today). | S28 (2026-07-21), Slice C/D code review (finding #5) | Revisit if/when a scheduler exists (move the harvest to a cron/confirm-time job) or if `recipe.list` perf degrades. Accepted for V1. | `open` |

---

## Resolved

| ID | Sev | Summary | Found | Resolved | How |
|----|-----|---------|-------|----------|-----|
| BUG-051 | 🟠 | **The service worker cached nothing on a route's first visit, so a freshly installed PWA opened once and then failed offline.** A worker does not control the page that registers it — that navigation is already in flight when `register()` runs — so the first visit to a route never reaches the `fetch` handler. The worker then activates and reports itself healthy holding **nothing**. | S60 (2026-08-01), OF1/OF2 failing `ERR_FAILED` on the offline reload | S60 (2026-08-01), 1F/C | ⚠️ **Invisible in a browser tab and fatal in an installed app**, which is why it had to be caught by the specs rather than by reading the code: in a tab you come back tomorrow and the second visit caches it, so nothing ever looks wrong. In a PWA the sequence is install → open once → walk to the shop → **the icon opens a dead page**, on the launch that matters most. Fixed by warming the four tab routes on `activate` (after `clients.claim()`), through the **same `isCacheable` gate** so a worker activating while signed out fetches four redirects to `/login` and stores none of them. ⚠️ A second bug rode along and would have made the fix look like it had not worked: the cache fallback needs **`ignoreVary`**. Next sets `Vary` on route responses, and a warmed entry is fetched as a plain GET while a reload is a **navigation** — so a Vary-respecting match misses an entry sitting right there, and that miss is indistinguishable from an empty cache. |
| BUG-050 | 🟡 | **The Recently-cooked strip rendered `COOKED JUL 29` while the identical badge in the rows below it rendered `Cooked Jul 23`** — same string, same `--spec-success` green, same pill, same padding, both on screen together. | S60 (2026-08-01), `/visual-qa` Layer A | S60 (2026-08-01), same pass | **B8b (S58) caused it, and the cause is the transferable part.** It routed the badge off `text-[10px] font-semibold` to `.spec-label`, which at 10.5px/700 is dimensionally the nearest rung and a reasonable way to pick one — but **both caps rungs carry `text-transform: uppercase`**, so applying one is a **casing** change, and casing is precisely the property a size-and-weight comparison cannot see. The source still reads `Cooked {cooked}` either way. The same sweep sent the `recipe-card.tsx` instance to `.spec-meta` and it stayed quiet: **one object, two answers, and nothing compared them.** ⚠️ **`caps-rungs.test.ts` could not have caught it — it is one-directional**, asserting that things wearing caps tracking route *through* a rung, with no way to see something routed *to* a caps rung that should not shout. 717 unit, 132 E2E and S58's own full `/visual-qa` all went green with both renderings live. Fixed to `.spec-meta` (§05: *"facts about the row above"*), guard added and **verified failing against the pre-fix tree** by physical backup + `git show 4e277a8:`. **Second instance of S57's rule:** which rung a slot takes is answered by what it HOLDS, not by what its type measures. |
| BUG-049 | 🟠 | **Text inputs under 16px zoom the iOS viewport on focus and never zoom back — on the phone-first surface, including both grocery edits you make standing in a shop.** Filed offenders: `recipe-header.tsx` (recipe search, 14px), `picker-content.tsx` (picker search, 14px), `freeform-field.tsx` (the §09 control's textarea, 14.5px — **on-ladder and still wrong**), `grocery-row.tsx` (item-name edit, 15px) and `grocery-row.tsx` (quantity edit, 13px). | S58 (2026-08-01), measuring the type scale for 1F/B8b | S59 (2026-08-01), 1F/C | ⚠️ **It was SIX sites, not five, and the reason is the finding.** `chip-adder.tsx` (the You tab's preference adder) uses `text-sm`; all five filed sites use `text-[Npx]`. The S58 measurement was hunting **arbitrary-value** sizes, so the one input written with a Tailwind named size was invisible to the method that found the others. **A filed list inherits the blind spot of the measurement that produced it.** ⚠️ **The tracker's own recommended fix was the thing to distrust — sixth session running.** It said *"copy `ui/input.tsx`'s `text-base md:text-sm` to all five"*, and copying a string to five call sites is precisely how the bug happened: the 16px floor existed in exactly one primitive, **privately**, so nothing could inherit it. Shipped as a named **`.spec-input`** instead — font-size only, no `md:` step-down (a phone app whose desktop rendering is incidental does not need two answers), with `ui/input.tsx` routed to it too so there is ONE answer. **Fifth instance of "an unnamed rung is a defect generator"** after `ui/button.tsx` (B3), `ui/textarea.tsx` (B7), the caps rungs (B8a) and six of §05's ten (B8b). Two new guards in `type-scale.test.ts`, **verified failing against pre-fix code** by physical backup + `git show main:` — red on exactly the three predicted sites. ⚠️ The first version of the sweep read JSX tags with `[^>]*` and reported four **already-fixed** sites as offenders, because `onChange={(e) => …}` ends the match before the className; it reads by brace depth now. **A guard that stops early does not under-report, it MIS-reports.** And it failed against its own comment (fourth instance after `palette.test.ts` and `caps-rungs.test.ts`), so it strips comments before asserting. |
| BUG-045 | 🟡 | **The quick-add dedupe notice was amber `#FF9F0A`, the last one in the product — the same miscast B5 removed from the merge marker, one affordance over.** Amber is the chef (§01), and the spec has deliberately no caution hue *because* amber is the chef; "that's already on your list" is the app reporting a mechanical fact, so amber said the chef was speaking when the chef was not. Law 03 also forbids colouring anything you simply read. | S52 (2026-07-31) | S57 (2026-08-01), 1F/B8a | Flat meta type (`--spec-text-muted`), which is what it is: a fact about the row above. 12 → 12.5px took it onto the §05 Meta rung while the line was open. ⚠️ **The `palette.test.ts` allow-list is GONE, not emptied** — listing the exact `file:line` is what reddened the test the moment the bug closed, so the permission could not outlive its reason (S52). That test now asserts zero amber anywhere. |
| BUG-048 | 🟡 | **The constraint chip's remove `×` was 20×20 against spec §12 item 05's 44px floor — the last hit-target violation in the app**, and the one `SH2` was allowed to find. Parked in S56 as a chip redesign: the chip is 36px tall, so a 44px target inside it looked like it would change every chip on the You tab. | S56 (2026-07-31), fixing SH2's blind spot | S57 (2026-08-01), 1F/B8a | **It was not a redesign, and measuring said so.** S56's Groceries drag-handle trade transfers: a real 44px box with an equal negative margin, so the box the layout sees is the 20px it always was and the chip stays 36px. It survives the wrap because the overflow lands in dead space — all three call sites wrap at `gap-2`, so the target overhangs 6px into an 8px column gap and 4px into an 8px row gap, with **no target-on-target overlap** (a neighbouring chip's body is not interactive). The hover fill stays on a 20px inner span so nothing is visible. ⚠️ The `data-hit-target-exempt` attribute went with it, and `SH2`'s allow-list is now asserted EMPTY — granting a future exemption is deliberately a two-place change. |
| BUG-047 | 🟡 | **The picker's search placeholder advertised the whole library while search was scoped to the room you were standing in.** `picker-content.tsx` built the placeholder from the `everything` tile's count regardless of which door was pushed, contradicting S48's own rule one control over — *SEARCH STAYS INSIDE THE ROOM IT IS STANDING IN* — so inside `Cooked` the field promised to search **5** and searched **2**. | S55 (2026-07-31), `/visual-qa` on the B6 captures | S56 (2026-07-31) | **Counts the room: `tiles.find(t => t.key === (tile ?? "everything"))`.** On the opening tier there is no pushed door and search genuinely does reach `everything`, so the library count stays honest there; derived from the same tile list the doors are drawn from, so the two can never disagree. **New `L19`, verified failing against pre-fix code** via a physical file backup — the red read `Expected: 2 / Received: 5`, the defect verbatim. ⚠️ **The spec reads BOTH numbers from the DOM** (the advertised count against the actual `picker-row` count) and asserts they differ, because a hand-fed number would assert nothing (S55). ⚠️ **Its first two runs failed for a reason worth keeping:** the picker renders its doors — `All · 0 recipes` — while `recipe.list` is still in flight, and the placeholder deliberately drops the number rather than say `Search 0 recipes`. **Waiting on the doors is not waiting on the data**; the spec waits on rows. ⚠️ **BUG-047 was NOT a §09 site**, as the S55 handoff had it: §09 says in as many words that *search is not this control*, so it rode along as a correctness fix rather than a fourth control to rebuild. |
| BUG-046 | 🟡 | **Three non-pressable elements on the recipe detail body wore the action hue.** `recipe-view.tsx` used `bg-primary` on the ingredient bullet and `text-primary` on both the step-duration meta and the `Modified` badge. Since 1E.7 aliased `--primary` → `--spec-action` all three resolved to cream `#F4EBDC`, and §01 says cream is what you press — B1's exact finding one surface over. | S53 (2026-07-31), closing B9 | S55 (2026-07-31) | **Fixed per element, never as a token sweep** — which is what the row asked for, and the fourth `text-primary` on the same screen is why. The bullet → `--spec-text-muted` (a list marker is structure, not an affordance); the step duration → `text-muted-foreground`, matching the step number already drawn beside it; the `Modified` badge drops its colour and inherits the muted meta row it sits in. **`View original source` keeps cream**: it is a real link and now the only cream on the screen, which is what makes the other three legible as *not* pressable. ⚠️ **The rubric's tracked entry went with it** — `visual-qa-rubric.md` now grades cream on a non-pressable element in the recipe body as a **law 02 finding** rather than a known gap, per S52's rule that a licence to ignore outlives the reason for it. |
| BUG-042 | 🟠 | **Email became an authorization boundary, and the parked instruction to close it was destructive.** S43's `ALLOWED_EMAILS` gate trusts the `email` claim to decide who may hold an account, and GoTrue's email provider is enabled by default — so a signup claiming an allowlisted address could in principle walk through Gate 2 without ever controlling that inbox. | S43 (2026-07-28), access-gate security review | S53–S54 (2026-07-31) | **Closed as WON'T-DO, on Griffin's explicit call (S54): do not disable the Email provider.** Two independent reasons, in order of weight. **(1) Not exploitable — measured, S51.** `GET /auth/v1/settings` returns `external.email: true` with **`mailer_autoconfirm: false`**, so confirmations are required; the bypass needs both halves and has one. **(2) The remedy was worse than the defect — measured, S53.** The premise *"the app has never used that path"* is FALSE: `mintSupabaseSession` signs the harness in with `signInWithPassword` (`tests/e2e/harness/supabase-session.ts:160`), which rides the email provider, and GoTrue rejects a password grant with `<provider>_provider_disabled` **before** it checks credentials — verified against this project's already-off **phone** provider (`422 phone_provider_disabled`) versus the enabled email one (`400 invalid_credentials`). Same code path, symmetric check. **Disabling Email would have turned all 123 specs red.** The harness's own cold-start error string already read *"Check that the Email provider is enabled"*; nobody connected it to this row for four sessions, and it was handed back to Griffin unexamined twice in S52. **The residual hygiene concern now lives entirely in the separate non-prod Supabase project decision** (`open-questions.md`) — that project is what would let prod disable Email while the harness keeps it on where it lives. Re-plumbing the harness off password sign-in is the only other route and is expensive: the admin API is unusable on this project's new-format keys + ES256 JWTs (`403 bad_jwt`). ⚠️ Separately, and NOT what this row was about: the same probe shows `disable_signup: false` + `google: true` with both gates unset by the S49 no-beta call — **prod is open to anyone who finds the URL, via Google.** That is deliberate; this toggle never changed it. **Fifth and sharpest instance of the phase's lesson: a parked recommendation is a hypothesis, and this one was not merely wrong but destructive.** |
| BUG-038 | 🟡 | **The recipe detail screen rendered labelled but empty `INGREDIENTS` and `STEPS` cards when a recipe carried neither**, plus ~800px of void above the floating verb. Reachable in production via an un-hydrated plan draft or a URL import that failed to parse. Two empty labelled boxes claim there is a section to read and then say nothing — BUG-009's class on a different surface. | S47 (2026-07-30), `/visual-qa` Layer A | S53 (2026-07-31) | **Each section is omitted when its array is empty; when BOTH are, one line of flat muted type** (`No ingredients or steps on this one yet.`) centred with `py-12`. No fill and no border, because law 05 reserves a container for something that responds to a tap. The copy **invents no cause** — from the screen, an un-hydrated draft and a failed import are indistinguishable — and offers no retry, because the recipe LOADED fine, so this is an empty state rather than an error one. ⚠️ **The filed bug was the smaller half.** `seed.ts` hard-coded `ingredients: []` / `steps: []` for EVERY recipe the Recipes seeder produced, so this was not an edge case in the harness — it was **100% of seeded recipes**, and `recipes-detail-add-to-week`, the tab's only detail capture, **had never once shown a populated recipe body**. The visual gate was grading the degenerate state as the canonical one, and fixing this bug alone would have made it BLINDER (that capture would have become the fallback, graded as normal). `SeedRecipe` now carries real bodies with exactly one recipe left deliberately body-less, plus a new `recipes-detail-empty` capture state. **RC14 verified failing against pre-fix code** via a physical file backup — its accessibility snapshot showed `heading "Ingredients"` and `heading "Steps"` rendered over nothing, the defect verbatim. RC14 asserts in **both** directions, because "the empty recipe hides the cards" alone would also pass against a build that deleted the sections outright. |
| BUG-037 | 🟠 | **`Add to this week` rendered as a dead grey control while `plan.current` was in flight.** The verb is `disabled` until the plan query resolves — deliberately, and correctly: answering a fast tap from "no data yet" would send someone with a live week to the intent screen. But the whole treatment was `disabled:opacity-60` with no other signal, so the Recipes detail screen's ONE floating primary photographed as unavailable rather than loading. | S47 (2026-07-30), `/visual-qa` Layer A | S53 (2026-07-31) | **The refusal stays; it now says so.** The verb reads **`Checking your week…`** while the query is in flight, then resolves to `Add to this week`. Took the tracker's cheaper option rather than the await-inside-`handleClick` rewrite, because the reasoning for refusing the tap was already right and only the silence was wrong. ⚠️ **A second gate-blindness closed with it, and it was self-inflicted:** `recipes-facts.ts` waited on `[data-testid="add-to-week"]:not([disabled])` before shooting — a workaround that stepped around this bug and made the visual layer structurally unable to see it. Now two distinct capture states: `recipes-detail-checking-week`, held open by a **routed delay** rather than a network throttle so it does not depend on machine speed (S50's rule), and the settled `recipes-detail-add-to-week`. **RC15 verified failing against pre-fix code**; the red showed the `disabled` button still reading `"Add to this week"`, exactly as predicted. |
| BUG-018 | 🟠 | **The E2E harness deleted rows in the real Supabase project, guarded only by a household NAME.** `seed.ts` runs deletes over a direct Postgres connection against production, and the sole barrier was `assertTestHousehold()` — the household's `name` equals `"E2E Test Kitchen"` and it has exactly one member, the test user. No environment check on `DATABASE_URL`, no DB-level privilege limit. S37 had already seen the harness's auth user vanish mid-run during a concurrent session. | S37 (2026-07-25), `/code-review` security lens | S51 (2026-07-30) | **The cheap fix, as scoped — but built as an ALLOW-LIST rather than the is-this-production test the row named.** No property of a URL says "production", so inverting the question would make the guard fail OPEN on every project it did not recognise, which is the opposite of what it is for. New `tests/e2e/app/project-guard.ts` parses the Supabase project ref out of BOTH `NEXT_PUBLIC_SUPABASE_URL` and `DATABASE_URL` (pooler form, where the ref is a suffix on the username, and direct form, where it is a label in the host), requires the two to AGREE — a half-edited `.env.local` would otherwise have the app talking to one project while the seeder deleted rows in another — and requires the result to be in a committed allow-list. **The ref is committed on purpose:** the failure mode this row names is a MISCONFIGURED `.env.local`, and a guard living in that same file cannot catch it; the ref is not a secret either, since it is the host in `NEXT_PUBLIC_SUPABASE_URL` that ships to every browser. Wired at MODULE LOAD in `tests/e2e/app/env.ts`, so it fires before Playwright builds a project list — no server started, no connection opened. **Verified by running the suite pointed at a foreign ref:** it refuses with a named error at config load. 10 unit tests. Two config lines came with it — vitest's `tests/e2e/**` exclusion narrowed to the Playwright FILE PATTERNS, and the harness config's `testMatch` pinned to `.spec.ts` — because a pure harness helper was previously unreachable from either runner, so it could not have had a test at all. **The separate non-prod Supabase project remains open as a decision, not a defect** (open-questions.md). |
| BUG-013 | 🟠 | **`finishOnboarding` persisted memory text the CLIENT wrote, stamped with the provenance the You ledger reads back as "You told me when we started".** For each deep answer the mutation stored `answer.memory` verbatim — a string the client computed, bounded only by `z.string().max(300)` — while the code's own comments described it as deterministic server-side synthesis from typed answers. Scoped to the caller's own household and treated as untrusted user-role content downstream, so provenance/data-integrity rather than prompt injection. | S37 (2026-07-25), `/code-review` security lens | S51 (2026-07-30) | **The tracker's own recommendation, applied literally, would have made it worse.** Recomputing from `(questionId, values)` lands in `memoryForAnswer`, whose label lookup fell back to the RAW value (`?? v`) — so 12 values x 60 chars of caller text would have been echoed into the sentence, more than double the 300-char cap the filed bug had. Verified: the naive version was implemented first and the values test went red against it. **Three parts.** (1) `deepAnswerSchema` drops `memory` AND `dimension`; zod strips unknown keys, so the client posts the state it holds and neither field reaches the server at all — stronger than ignoring them, because nothing is left to start trusting again. (2) `synthesizeMemories` resolves the question out of `DEEP_QUESTIONS` by id, and `memoryForAnswer` now filters `values` to the options that question actually offers (de-duplicated), so the sentence is built entirely from the server's own labels; the category is read off `question.dimension`, so a caller cannot file a preference as a behavior. The filter lives in `memoryForAnswer` rather than at the call site so client and server keep producing the identical sentence. (3) A second, quieter echo in the same class: `synthesizeHeadlineMemory` did `DIET_LABEL[x] ?? x` on a field this input types as a free string — it now DROPS a framework it has no label for (the schema mismatch itself is logged as BUG-044). Also removed the `as InterviewState` cast the router used to reach synthesis, which is what had let a `string` field stand in for an enum unnoticed; `synthesizeMemories` takes a new `MemorySource` naming exactly what it reads. **6 tests, every one verified failing against the code it targets** — 4 against the pre-fix code, the values one against the naive fix, the diet one against the old `?? x` line. One of them initially could NOT fail: the proteins branch lowercases its list, so an uppercase marker never matched and the assertion walked past a string that had in fact landed. |
| BUG-011 | 🟠 | **`householdSize` and `householdComposition` could desync, putting two contradictory facts in the same chef prompt.** Composition → size is derived in one place, but `householdSize` had **three independent writers that never touched composition**: `user.updatePreferences` (bare scalar accepted), the You-tab stepper, and the `set_household` talk op. Nothing reconciled back, so `getChefContext` could render `Default servings: 4` beside "Cooking for 2 adults and 1 baby… under 6 months and not on solids". | S37 (2026-07-25), `/code-review` correctness + architecture lenses | S50 (2026-07-30) | **Griffin's call: the real fix, all three writers, not a read-time patch.** The tracker's "make `householdSize` read-only" recommendation had a hole — both non-onboarding writers send a BARE COUNT, and a count cannot say which band changed (2 adults + 2 children stepped 4→5 is either a third adult or a third child), so read-only alone would have broken them. Three parts: **(1)** `HouseholdComposer` extracted from the onboarding screen into `components/shared/` and used by the You-tab sheet, so the two surfaces edit the same fact through the same control instead of drifting; `normalizeComposition` moved to `lib/household.ts` because the server applies it too. **(2)** `set_household` is band-aware — the model sends the bands when the message names them, and bands it wasn't told about are preserved ("we've got a baby now" no longer erases the children). A bare total falls back to **landing on adults**, a stated guess rather than drift: an unspecified extra person is an adult, and adults are the only band that always counts toward servings. **(3)** `householdSize` is now READ-ONLY in `updatePreferences` — one writer, one derivation. Covered by **Y4/Y11** (the UI writer, incl. the first-foods baby that deliberately does not move the count), **Y12/Y13** (the talk writer, bands and bare total), and 9 unit tests on the apply logic. |
| BUG-012 | 🟠 | **The You tab rendered "4 adults" for a household of 2 adults + 2 children.** `soft-constraints-card.tsx` printed `householdSize` with an "adults" label, but `householdSize` stopped meaning adults in S36 — it is a derived servings count folding in children and 12-24mo babies. Directly undercut the tab's job: it is the surface where you verify the chef isn't wrong about you. | S37 (2026-07-25), `/code-review` architecture lens | S50 (2026-07-30) | New `householdRoster()` in `lib/household.ts` — `describeHousehold` suppresses the adults-only case because the chef PROMPT already says "Default servings: N", but a UI label has no companion line and needs the roster in every case. With **no composition on file** the count is named as what it actually is ("2 servings") rather than dressed as a roster, so BUG-010's honesty holds on the display side too. The E2E seed was changed to a MIXED household (2 adults + 1 child) — the exact shape that rendered as "3 adults" — so Y1 now fails if the label regresses. |
| BUG-010 | 🟠 | **`household_composition` shipped with a column DEFAULT, so a default was indistinguishable from an answer.** Migration `0006` gave the column `'{"adults":2,"children":0,"babies":0,"babyStage":null}'`, so every pre-existing row and every row created by an unrelated write carried a composition byte-identical to a real reply. Same class as the `dietary_framework` default that Layer B caught lighting "No restrictions" over an unanswered question (fixed S37). | S37 (2026-07-25), `/code-review` architecture lens | S50 (2026-07-30) | Migration `0010` drops the default; the column is nullable with NULL meaning "never answered". `DEFAULT_HOUSEHOLD_COMPOSITION` stays as the CLIENT's stepper starting state, which is a different thing from a stored fact. **Existing rows were deliberately NOT backfilled to NULL:** a genuine "2 adults, no kids" answer is byte-identical to the default, so nulling them would destroy real answers to fix a cosmetic inconsistency. `updatePreferences` accepts an explicit null so an UNDO can restore never-answered, and nulls `householdSize` alongside it — a stale servings count beside an unknown composition is the same contradiction in a quieter form. |
| BUG-020 | 🟠 | **`retryFailed` retried FAILED saves but never awaited IN-FLIGHT ones, so "All saved." could be reported over a write that had not landed.** A save still in flight had neither succeeded nor failed, so it was in **neither bucket** and `retryFailed` returned `true` the moment `failedSaves` was empty. Tap through the last core turn onto reflect and press "Plan my first week" fast enough and `finishOnboarding` succeeded, the completed flag was written, and the pending `updatePreferences` then failed with nothing left to retry and no surface to report on. BUG-016's honest-about-saves contract reached by the one path that fix did not cover — and because the flag makes the interview fire **exactly once per account**, the lost answer was gone for good. | S40 (2026-07-26), 1E-close code review | S50 (2026-07-30) | **The third bucket.** `useCoreSaves` now tracks every settled save promise in an `inFlight` set; `retryFailed` **awaits those first**, and only then asks whether anything failed. That ordering is the whole fix — the old code asked the question first. A second, subtler part was required to make it work: `retryFailed` awaits saves whose failure handlers call `setFailedSaves`, and a state update from an awaited callback is **not visible to the closure that awaited it**, so the failures list is mirrored into a ref that is written synchronously and read after the await. Covered by **OB16** (blip: the answer is retried and lands, rather than being silently lost — the assertion that falsifies the bug) and **OB18** (connection gone: the interview stays put and the once-per-account flag is NOT written). Both hold the in-flight window open with a **test-released gate** rather than a timer, because a spec that reproduced this interleaving only on a slow machine would be worse than none. Both verified failing against the pre-fix code. |
| BUG-021 | 🟠 | **`skipAll` walked the user out to Plan even when `skipOnboarding` failed** — the exact opposite of the fix BUG-016 landed on the finish path. `mutate(undefined, { onSuccess: leaveToPlan, onError: leaveToPlan })` routed both outcomes to the same place, so a failed skip left `onboardingCompletedAt` NULL and the first-run gate dropped the user back into the interview on their next load with nothing explaining why "Skip for now" had not stuck. No data is lost (unlike a failed finish), which is why 🟠 — but a control that silently does not work is a trust bug on a first run. | S40 (2026-07-26), 1E-close code review | S50 (2026-07-30) | Mirrors `finish`: `onError` stays put and toasts *"I couldn't skip just now. Tap again and I'll retry."* Covered by **OB17**, which also proves the retry is the same control and that the second attempt lands. Verified failing against the pre-fix code. |
| BUG-035 | 🔴 *(logged 🟡)* | **A generation that died produced NOTHING on screen — no error, no retry, no spinner.** Logged as "a real generation timed out at 90s, server-side" with two open questions. Both answers were wrong in the tracker. **(1) There is no 90-second timeout anywhere in the app.** The only `90_000` in the repo is the Playwright `waitFor` ceiling in `plan-live.capture.ts`; the harness reported *its own* wall and S45 recorded it as the server's. The real abort was ours, `AbortSignal.timeout(60_000)`. **(2) The failure card was unreachable on EVERY path, not just the guarded one.** `renderBody` gated it on `streamError && !plan`, but `useObject` only sets `error` for a failed *request* — the route has already returned 200 and opened a body by the time a stall happens, so a dead stream arrives as a body that simply CLOSES: `isLoading` false, `error` undefined, nothing rendered. On a first run the user was dumped back to the intent screen with their typed request erased; on a regenerate the OLD plan silently reappeared (a timeout never reaches `persistPlan`, so `plan` stays truthy). A silent no-op, BUG-019's class, and the more likely path in real use. **Also found: `maxDuration` (60) EQUALLED the AI abort (60)**, so on Vercel the platform's kill and our timeout landed at the same instant and the route had no time to render its own failure. | S45 (2026-07-29), Layer B round 1 | S50 (2026-07-30) | **Four parts.** (1) **Detection** — `onFinish({object, error})` is the honest signal; `object === undefined` means no valid plan arrived, whatever killed it. (2) **Two surfaces, Griffin's option B** — with no plan the failure owns the screen; **with a plan on file it borrows §C's slot and the week stays put**, because you asked for a new week and did not get one, which is a failed action rather than a reason to take away the week you had. Retry replays the ORIGINAL payload (rebuilding it would silently drop the recipes the person picked). (3) **One silent server-side retry, before the first token only** (`withStreamRetry`) — at the model layer because a client retry is a second request that re-runs `consumeDailyAiBudget` and double-bills the daily cap, and because past the first token the client holds half a JSON document that a second attempt would corrupt. (4) **Timeout ladder 45s attempt → 100s outer → 120s `maxDuration`**, each strictly slower than the one it contains, pinned by `config.test.ts` which **scrapes `maxDuration` out of the route source** so the two can never drift into a tie again. Tests: 8 unit on the wrapper, 7 on the ladder, 4 on the waiting copy, and E2E **X3–X6** (X6 drives a real timeout in 2.5s via an E2E-only per-attempt override that prod cannot honour). X3 and X4 both failed before the fix. |

| BUG-041 | 🟠 | **§B's boundary sentence — *"it's your recipe, so I won't rewrite it"* — was asked for and did not appear.** `buildPicksBlock` asked the model to state it; two live Layer-B rounds (the second with both real output fields named) produced it **zero times**. Meanwhile the test layer hardcoded the sentence in three places — the seed state, the mock fixture, and an E2E assertion — so Layer A photographed it and the suite proved it while no real user would ever see it: fixture drift making the suite green against a lie. | S47 (2026-07-30), Layer B rounds 1 + 2 | S48 (2026-07-30) | **Griffin's call: stop asking, per BUG-033's precedent (third instance of the class).** The prompt clause is deleted; the boundary renders as **product copy on the picked row** (`meal-row.tsx`, `picked-boundary`) — *"Your recipe — the chef won't rewrite it."* — caption colour, not gold, because gold marks the chef speaking and this is the product's own guarantee. Deterministic, which is the one thing a guarantee has to be. The seed's `chefNote`, the mock's `chefResponse` and L4 were all rewritten to carry what the model actually produces; the behavioural half of the rule (*do not rewrite/rename/substitute*) stays in the prompt, and the model obeys that half. |
| BUG-040 | 🔴 | **The chef printed internal `Day N` vocabulary straight to the user, on the pick path.** Real output: `chefResponse` = *"Placed Congee with Ginger and Scallion on **Day 0** to start the week…"* on the Recipes detail screen, and a picked meal's rationale = *"This dish fits perfectly on **Day 1**…"* on the Plan rail. **BUG-031's exact defect, second door.** S45 fixed the day map for GENERATION and nobody asked whether MODIFY had the same hole — it did: `modify-plan.ts` addressed the week as `Day 0:`/`Day 1:` and supplied no weekday names at all, so the model wrote back the only day vocabulary it had been given. It was never disobeying; it was echoing us. | S47 (2026-07-30), Layer B round 1 | S47 (2026-07-30) | Structural, mirroring the BUG-031 fix: `modifyPlan` now takes `weekStart` and carries `buildDayMap`'s real day map (exported from `generate-plan.ts`), and `PLAN_MODIFY_OUTPUT_RULES` forbids writing a dayOffset into `chefResponse` or a `rationale`. Two tests assert the real mapping (a Sunday-start week, so a Monday-start assumption fails) and the output rule. **Verified live in round 2.** |
| BUG-036 | 🔴 | **Every `.glass-card`, `.glass-surface` and `.glass-sheet` in the app shipped with NO backdrop blur in Chrome and Android — since 1E.7.** Those three classes hand-wrote `-webkit-backdrop-filter` beside the standard property; `.spec-chrome` and `.spec-floating` did not. lightningcss collapses the duplicate onto the **prefixed** form and drops the standard one, and Chrome removed `-webkit-backdrop-filter` years ago — so the built CSS carried a blur no current browser honoured. The fill stayed correct at `.94`, which is exactly why it read as deliberate flatness rather than as breakage, and why nine sessions of visual QA walked past it. **This is BUG-022's real cause.** | S47 (2026-07-30), `/visual-qa` Layer A — chased down from phantom meal rows reading through the picker's own list | S47 (2026-07-30) | Deleted the three hand-written prefixes and let the toolchain prefix from browserslist; the built CSS now emits both forms. Guarded by `src/app/globals.test.ts`, which fails on any hand-written `-webkit-backdrop-filter` and on any glass class that loses its standard declaration. Verified in the built stylesheet and in the pixels. |
| BUG-022 | 🟡 | **The tab bar ghosts through an open bottom sheet.** ⚠️ **The original diagnosis was wrong, and instructively so.** S42 attributed this to the spec's *deliberate* `.94` L5 translucency and routed it to 1F on that basis — *"do not raise the L5 alpha, `.94` is the spec's value"*. The alpha was never the problem: the **blur was missing entirely** (BUG-036), so 6% of an *unblurred* background was reading through instead of 6% of a blurred one. Attributing a symptom to a deliberate design value is what kept it parked for five sessions. | S42 (2026-07-27), 1E.7 `/visual-qa` re-capture | S47 (2026-07-30) | **Fixed by BUG-036**, and verified on its own original repro: `tests/e2e/captures/A-groceries-2026-07-30T17-15-47-770Z/grocery-chef-sheet.png` — the tab labels are no longer legible behind the textarea. No alpha was changed. |
| BUG-034 | 🟡 | **The chef's week summary runs six to seven lines and pushes the first meal below the fold.** Frame `3i` draws TWO strings at two sizes — a short claim at 22px cream and the argument at 14.5px italic gold — and `chef-header.tsx` had props for both. `week-wrapped-state.tsx` passed both; **`plan-review.tsx` passed only `summary`**, and generation emitted the single `chefSummary` the prompt asked for as "one or two sentences". So both of the model's sentences landed in the 22px heading and the gold slot rendered nothing, on Plan's main screen. An unwired field, not a copy-length problem. | S45 (2026-07-29), Layer B rounds 1-3 (all six weeks) | S47 (2026-07-30) | **Griffin's call: split the output.** New `chefNote` field (schema + prompt + `chef_note` column, migration `0009`) carries the argument into the `rationale` prop that already existed. The one-sentence ceiling lives in **code** (`splitChefVoice`), not the prompt — BUG-033 established that a style clause loses to the request competing with it. It **splits rather than truncates**: overflow past the first sentence boundary becomes the note, so nothing the chef wrote is lost. `absorbRepeatedMethod` now matches both halves, or the split would have silently disabled absorption. ⚠️ **Residual: the guarantee is one *sentence*, not one *line*** — a single long sentence is still unbounded, which `adversarial` now photographs deliberately. |
| BUG-039 | 🟡 | **D4 (drag-the-handle-to-dismiss) grabbed the sheet mid-open-animation** — green in isolation, red in the full sequential run, which is BUG-019's exact signature. `toBeVisible()` passes the instant vaul mounts the drawer, while it is still flying up from below. Measured mid-drag, the drawer's translateY ran **416 → 196 → 57 → 21** across a drag meant to move it *down*: the open animation was still winning, the pointer deltas were fighting it, and the position vaul saw at pointerup bore no relation to the distance dragged. Under load the animation is slower, which is why only full runs failed. | S47 (2026-07-30), full E2E run after the Layer A fixes | S47 (2026-07-30) | `dragSheetDown` now waits for the drawer's transform to settle at the top of its travel before touching the handle, and yields a frame between pointer moves — the same two-part fix S46 used on GR7. Diagnosed by probe rather than retried. |
| BUG-019 | 🟡 | **GR7 (grocery section drag-reorder) was flaky in a full-suite run — three recurrences, which tripped the tracker's own quarantine threshold.** Green in isolation every time; red only under a full sequential run; nothing in S38, S44 or S45 touched Groceries, and every other GR spec was green in the same runs. A flake is worse than a missing test: it trains you to re-run instead of read, which is exactly how a real Groceries regression ships. | S38 (2026-07-25); recurrence #2 S44, #3 S45 | S46 (2026-07-29) | **Fixed properly rather than quarantined — and the cause was a race, not timing noise.** The old helper pressed down, crossed the 8px activation distance, then fired ~24 more `mousemove`s back to back **without ever waiting**. `@dnd-kit` runs collision detection against a droppable-rect snapshot taken when the drag STARTS, so if React had not committed the drag-start render and measured the droppables before those moves landed, every move resolved against nothing, `onDragEnd` got `over: null`, and `onSectionDragEnd`'s first line returned early. **No mutation, no error, no request** — a silent no-op indistinguishable from a broken feature, and whether it lost the race depended on machine load, which is why only the full run ever failed. Three changes: `GrocerySection` now exposes `data-dragging` (the lift made observable — there is no `DragOverlay` in this build, so the state was only ever an opacity class, and asserting on styling would couple the suite to it); the helper **waits for that state** before moving, then yields a frame between moves so collision detection actually runs; and the assertion reads the **whole persisted aisle order**, compared against the order before the drag, so a no-op drag can no longer pass. Also scrolls both boxes into view — `boundingBox()` returns viewport coordinates happily even when they are off-screen, where the mouse cannot go. |
| BUG-031 | 🔴 | **The chef named a weekday that did not hold the ingredient, and often had not happened yet.** On a Wed→Tue week, Thursday's card read *"Uses the leftover fresh dill from **Monday**"* — a Monday four days in its own future, whose meal was fried rice and carried no dill. A second round put *"Brighten **Tuesday**"* on a Thursday. **The S40 fix caused this.** S40 caught "reusing olive oil from day 0" and fixed it by instructing the prompt to use WEEKDAY NAMES instead of offsets — but nothing ever told the model *which* weekdays, so it mapped `dayOffset` onto a Monday-start week (offset 1 → "Tuesday"). On any week not starting Monday — i.e. most weeks — every reference was wrong. That made the S40 fix a **downgrade, not a repair**: "day 0" looks like a bug and gets reported; "Tuesday" looks correct and quietly misinforms. | S45 (2026-07-29), Layer B rounds 1-2 | S45 (2026-07-29) | **Structural, not a constraint.** `buildPlanStreamParams` now takes `weekStart` and puts a real day map in the *user* message (`- dayOffset 0 is Wednesday, July 29 …`), plus "the week does NOT start on Monday". The system-prompt clause was rewritten to point at that map and to require an **earlier** day that **actually carries the ingredient**. The map rides in the user message because `buildPlanSystemPrompt` is asserted static. Locked by 3 tests, one asserting the whole seven-day map off a **Wednesday** start — a Monday fixture would have passed against the bug. **Verified live in round 3**: "Finish the leftover fresh dill from Wednesday", correct day, correct direction, correct ingredient. |
| BUG-032 | 🟠 | **The ingredient-reuse rule colonised the chef's voice.** Round 1's `live-fridge` came back with **7 of 7** rationales arguing waste ("reduce waste" / "avoid waste" / "using up perishables" / "using up remaining lemons" / "before it wilts" / "finish perishables" / "use up basil"); `live-healthy-weeknight` was 4 of 7. The rationale's one job is *why this meal, why this day* — a busy night, variety, the shopping run — and it had collapsed into a single argument repeated all week. S40 verified the reuse rule **works**; nobody checked whether it **dominates**. This is also the cause behind Layer A's standing "seven gold rationales read as texture, not voice" finding: the texture was real, and it was because they all said the same thing. | S45 (2026-07-29), Layer B round 1 | S45 (2026-07-29) | One clause: *"Reuse is a habit, not a theme: AT MOST TWO rationales in the week may argue about finishing an ingredient. Every other rationale argues why THIS meal belongs on THIS day."* Locked by an assertion on both halves. **Round 3: reuse-primary rationales down to 2 of 7 — exactly the cap.** Reuse still appears as a *secondary* clause in ~6 of 7, which is softer and arguably correct (a cook who is thinking about waste may mention it); flagged for Griffin's taste pass rather than tightened further, because the next turn of the screw starts costing the chef a real thought. |
| BUG-033 | 🟠 | **W1's title rule was marked ✅ in the scope table but never written into generation at all.** The ledger and scope-1E.5 both specify "titles never open with a cooking verb (enforced in *generation*)" and "a method covering four or more meals is absorbed into the week" — `PLAN_OUTPUT_RULES` contained neither. Found by grepping the prompt while judging the very Layer B run that existed to verify it. The live model duly produced `Grilled Lemon-Herb Chicken` and `Pan-Seared Salmon`. | S45 (2026-07-29), while judging Layer B round 1 | S45 (2026-07-29) | **Split by what each half can actually be enforced by.** The verb rule went into the prompt with its named-dish exception ("Grilled Cheese keeps its name"). The **four-or-more rule went into code** — round 2 asked for "I want to grill" *with the prompt rule in place* and returned **seven of seven** "Grilled X", reproducing S40's original finding verbatim. The model was not disobeying; a style clause cannot outrank the request it competes with, and "does one word open four or more titles" is a string test. New pure `absorb-method.ts` runs in `validatePlan`: it strips a leading method covering ≥4 titles, **only when `chefSummary` already says it** (absorption is a precondition for dropping — otherwise the strip deletes the only place the person could read it), never renames a named dish, and never touches a mid-title method. 6 unit tests, fixtured on round 2's exact seven titles. ⚠️ **Not exercised live**: round 3 returned zero method-opening titles, so the code path did not fire. |
| BUG-030 | 🟠 | **The Layer B capture spec was the one Plan file S44's BUG-024 migration missed — and it fails by spending money.** `plan-live.capture.ts` still waited on `getByRole("heading", { name: "Your week, ready to review" })`, the hero the rail deleted. `selectors.ts` and `expected-facts.ts` were both migrated in S44 (the former even documents `planRail` as the replacement anchor); this file was not. Every state ran its **real** generation, paid for it, then sat for 90s waiting on a string that no longer exists and returned `captureStatus: "error"` with no screenshot. A stale selector fails loudly and free everywhere else in the suite; here it fails silently and bills you. | S45 (2026-07-29), first Layer B run | S45 (2026-07-29) | Re-anchored on the **draft's decision** rather than a heading: `confirmBar` (`/^Confirm \d+ /`) appears only once the week is written and a decision is being asked. Deliberately **not** `planRail` — W5 makes the full rail arrive in the first second and resolve in place, so waiting on the rail would have screenshot a wall of provisional rows and called it a settled week. Same pass added per-slot `estCostCents` to the debug HUD and a `COST` readout to the run log, because W6's output was otherwise unobservable from outside: the review sums it into one string and a null slot renders nothing, so a badly-priced week and an unpriced one looked identical. |
| BUG-024 | 🟠 | **The Plan E2E specs selected against a DOM the 1E.5 rail replaced — 21 of 78 red.** Spec migration owed by an intentional rebuild, not a behaviour regression, but it left the Plan tab with **no mechanical gate**. | S43 (2026-07-27) | S44 (2026-07-28) | Migrated, not weakened — and the tracked cause list was **incomplete**. Three causes were logged; **five** were real. (1) `reviewHero`'s heading → the `data-testid="plan-rail"` anchor. (2) chips moved to the meal sheet → `runMealAction` opens the sheet first. (3) the in-card `Reworking …` label → the gold ring, asserted via **computed `box-shadow`** rather than a class name, because §C's rule is that the ring is *gold* and a class match would pass on a grey one. **(4, untracked) the standalone `Talk to the Chef` button is gone** — the whole-week door is the chef header's `Something's off` — which took M4/M5/M7. **(5, untracked) `Start over →` vanished from the draft screen entirely** (see BUG-026). Assertions came out **stronger**: M1 now proves the ring is on the row and nowhere else; M5 measures the toast against the confirm bar's real bounding box; M6 proves every chef action goes inert rather than that one tap was silently dropped. |
| BUG-006 | 🟡 | **Plan's expanded sheet showed the FULL recipe, not a summary** — `ExpandedMealSheet` rendered the whole `RecipeView` (ingredients + steps) inline, making Plan a second, worse Recipes tab. | S35 (2026-07-24), Griffin testing | S44 (2026-07-28), 1E.5 W7 | Rebuilt to the wave-1 settled structure: identity (day · meal · meta · title · gold rationale at feature size), then **one action model, two kinds of action** — *chips ask the chef, rows go somewhere* — grouped `ASK ME FOR A CHANGE` then `TAKE IT SOMEWHERE`. **Ingredients and steps never enter Plan**; one row leaves for Recipes and it **states its own state** (a recipe still being written is not a link, a failed one says so) rather than offering a door that goes nowhere. Covered by **P8**. Worth knowing: this is a real subtraction — reading the full recipe while reviewing the week used to happen here, and the ledger's trade is that it is one tap away in the tab that owns it. |
| BUG-008 | 🟡 | **The Plan meal card printed the cook time twice**, and two *different* times when `estTimeMinutes` and a time-shaped tag disagreed ("95 min · … · 90 min"). | S37 (2026-07-25), ADVERSARIAL Layer-A capture | S44 (2026-07-28), 1E.5 W1 | Broader than tracked: the meta row **had no contract at all** — it appended `estTimeMinutes`, then servings, then every tag verbatim. `rail-helpers.metaLine` now emits one cook time and one serving count; a time-shaped tag is **dropped rather than deduped**, because a card can state one cook time honestly and `estTimeMinutes` is the structured one. Non-time tags become markers above the title, where they can never read as a second duration. Unit-locked in `rail-helpers.test.ts` (both the disagreeing and agreeing cases) and now **gated by P3** against the ADVERSARIAL seed. |
| BUG-009 | 🟡 | **A slot with no title rendered as a permanent "Thinking…" card** — a settled plan presenting a forever-loading state. | S37 (2026-07-25), ADVERSARIAL Layer-A capture | S44 (2026-07-28), 1E.5 W5 | A titleless cookable slot is now the **provisional row**: the spec's hueless `.09` fill with a `.2` line, a muted sentence where the title goes, **never a spinner**. The same object serves "not written yet" and "deliberately undecided" because the user-facing fact is identical — nothing is planned here and nothing was bought for it — which is also why generation needs no separate streaming vocabulary. It holds its height, so resolving changes text and fill only and nothing reflows. **Confirm stays live over it**, and the count says four dinners, not five, so a hole can never be mistaken for something the shop covers. Gated by **P4**. |
| BUG-025 | 🟠 | **The rail's floating slot was `absolute`, so it scrolled away with the content.** The frame's "bottom 96" is a distance from the screen edge; on a phone canvas those are the same thing, but the app's scroll container is the document, so `absolute bottom-24` anchored to the bottom of the *content*. On a seven-day draft, `Confirm 7 dinners` was only reachable by scrolling to the very end — and §D calls for exactly one floating layer above the tab bar, which a layer you have to hunt for is not. The retired `BottomBar` had used `fixed` and was viewport-anchored. | S44 (2026-07-28), migrating BUG-024 — surfaced as Playwright "element intercepts pointer events" on RG1 | S44 (2026-07-28) | `SLOT` is now `fixed`, using the tab bar's own centring trick (`fixed` positions against the viewport, which costs the column its `mx-auto`). Geometry is unchanged: bottom 96, height 52, radius 16, insets 16. **M5 now measures it** — it takes the confirm bar's bounding box, fires a modify, and asserts the toast matches on all four dimensions. |
| BUG-026 | 🟠 | **The rebuild dropped the regenerate airlock's draft door.** The chef header carries ONE revise control and frame `3i` spends it on `Something's off` (→ Talk to the Chef), so on a draft there was **no way to re-prompt at all**. The brief's state inventory §3 explicitly keeps that door ("Start over" on a draft, "Plan a new week" on a confirmed week), and scope-1E.5's own acceptance criteria require RG1–RG5 not to regress. A feature loss, not a spec problem. | S44 (2026-07-28), migrating BUG-024 — RG1/RG3/RG4/RG5 | S44 (2026-07-28) | Restored as the **foot link under the rail**, which is the pattern `plan-midweek.tsx` already uses for the same job — so it adds no new object and does not breach §D's one-floating-layer rule. Draft only; on a confirmed week the header's control *is* `Plan a new week`. **Griffin's call is open on placement**: frame `3i` does not draw the bottom of the scroll, so this is a reading of the ledger rather than a locked decision. |
| BUG-027 | 🟡 | **§D's scroll padding (168px draft / 108px confirmed) was never built**, so nothing below the rail could clear the floating primary. Compounded BUG-025. | S44 (2026-07-28) | S44 (2026-07-28) | `SLOT_PADDING_DRAFT` / `SLOT_PADDING_BARE`, named beside the slot they exist for and applied by `PlanReview`, `PlanMidweek`, `StreamingPlan` and `WeekWrappedState`. |
| BUG-028 | 🟠 | **The Plan tab had no heading at all.** Deleting the "Your week, ready to review" hero took the surface's only `<h2>` with it — the chef's claim replaced it as a `<p>`, so assistive tech had no landmark on the app's signature screen. | S44 (2026-07-28), rebuilding week-wrapped | S44 (2026-07-28) | `ChefHeader`'s summary is an `<h2>`. The type is unchanged; only the element is. It is the screen's claim on every Plan state, so this restores the landmark everywhere rather than patching one screen. |
| BUG-029 | 🟠 | **A failed modify became unreachable once the user walked away from the sheet.** The rail moved every modify into a sheet, a failing sheet deliberately stays open, `ModifyStatusPills` was gated on `!sheetOpen`, and dismissing the sheet called `clearError()` on the way out — so the scroll-independent retry (X1's whole mechanic) could never render. | S44 (2026-07-28), migrating BUG-024 — fixme'd rather than weakened, then fixed | S44 (2026-07-28), 1E.5 W3 | The error lives in the **action bar's slot**, which does not care whether a sheet is open, and dismissing a sheet no longer clears it (a fresh action still does). X1 is now **stronger** than the test it replaced: it asserts the failure *survives* the dismissal, which is the part that was broken. |
| BUG-016 | 🔴 | **A failed preference save during onboarding was silent, and the interview still said "All saved."** The four core turns fired `savePreferences.mutate(...)` with no `onError`, and `finishOnboarding` routed both outcomes to the same place. A dropped connection lost that answer — or every synthesized memory — with no indication and no retry, on a flow that fires exactly once per account. | S37 (2026-07-25), `/code-review` perf/UX lens | S39 (2026-07-25) | New `use-onboarding-saves.ts` (`useCoreSaves`) owns core persistence: a failed save is **named** in a toast, **remembered** keyed by the answer's label, and **retried** when the user taps "Plan my first week" — the natural end point, by which a transient drop has usually healed. The reflect screen no longer claims "All saved" while anything is outstanding (`onboarding-unsaved-note`). A failed `finishOnboarding` now **stays on reflect** with a live retry instead of walking the user out to a plan built on nothing — that path saved neither the memories nor the completed flag, so leaving silently just deferred the loss to the next load. Deliberately does NOT block each turn on its round trip (that trades a rare failure for a stutter on every answer). Regression-guarded by **OB15**, which aborts the first `user.updatePreferences` for real and asserts the dropped answer reaches the database. |
| BUG-015 | 🟠 | **Nothing disabled the tap controls while a free-text capture was in flight, so two writes raced.** Type "we're vegan", tap Pescatarian, confirm before the reply lands: two mutations hit the same field and the winner was whichever request finished last. | S37 (2026-07-25), `/code-review` perf/UX lens | S39 (2026-07-26) | The confirm on both the shared question screen and the household screen is now `disabled={talkPending}`. Disabled rather than hidden: the button was already on screen, and removing it from under a thumb is worse than dimming it for the second the reply takes. Chip taps stay live, since only the confirm writes. |
| BUG-014 | 🟠 | **Typed free text was cleared before the request resolved, so a failed capture lost what the user wrote** — while the toast invited them to try again. | S37 (2026-07-25), `/code-review` perf/UX lens | S39 (2026-07-25) | `submitFreeText` now resolves a boolean (`mutateAsync` in place of fire-and-forget `mutate`) and `TellMeField` clears **on success only** — the same rule `shared/talk-to-chef-sheet.tsx` already followed. A committed write whose read-back fails still counts as success, since the message did land. Regression-guarded by **OB14** (aborts `user.talk`, asserts the field still holds the text). |
| BUG-005 | 🟠 | **App-wide serif: `font-sans` never resolved to Geist.** Every heading and every line of prose on every tab rendered in the browser's default serif. | S33 (2026-07-22), You visual-QA — parked pending an on-device check, on the theory it might be capture-only | S37 (2026-07-25) | **Not capture-only, and no device check was needed.** `globals.css` had `--font-sans: var(--font-sans)` — a self-reference, invalid at computed-value time, so the token could never resolve anywhere; Geist was loaded in `layout.tsx` as `--font-geist-sans` with nothing consuming it. One-line bridge. Deliberately fixed inside 1E rather than deferred to the 1F design-system pass (the tracker's original disposition), because Griffin's taste pass on the onboarding copy was about to happen in the wrong typeface. Blast radius checked: Layer-A re-capture of Plan, Groceries, Recipes and You — no truncation or reflow damage on any surface. |
| BUG-007 | 🔴 | **E2E harness couldn't authenticate — `bad_jwt` on every Supabase admin call.** All 70 specs failed to run. | **Root cause was the harness, not the credentials.** The project uses the new `sb_publishable_`/`sb_secret_` key format with asymmetric (ES256) JWTs. Those keys are valid — PostgREST accepts the secret key, and ordinary password sign-in with the publishable key works — but GoTrue's `/auth/v1/admin/*` endpoints still expect a JWT carrying `role: service_role`, and `sb_secret_…` isn't a JWT, so they reject it. `mintSupabaseSession` was calling `auth.admin.listUsers` + `updateUserById` to bootstrap a user that **already existed**. Fixed by signing in with the publishable key first (which also drops a 50-page `listUsers` scan from every run) and falling back to the privileged bootstrap only when sign-in genuinely fails. No key change needed; **Griffin's `.env.local` was already correct.** | S36 (2026-07-25) — 70/70 E2E green after the fix |
| BUG-002 | 🟠 | **Merge produced duplicate name-lines** — the same canonical item on two rows: `Salt 3.25 tsp` + `Salt (to taste)`, `Black pepper 2.25 tsp` + `Black pepper`, `Carrot 1.5 lb` + `Carrot 0.5 cup`. Safe under-merge, but untidy to a shopper. | S28 (2026-07-21), real-model merge eval | **S31 (2026-07-22)** | Added a **buy-unit table** to the aggregator (`src/server/grocery/buy-units.ts`). Items a shopper buys as ONE thing consolidate to a single row: **staples** (salt, pepper, oils, dried spices, vinegars) collapse to one **unquantified** row — the measured tsp is shopping noise (Griffin's call: drop the number, the per-meal amounts survive in the amber-dot breakdown); **concrete buy-unit** produce (carrot→lb, onion→count, …) collapses to one row that sums the buy-unit and **absorbs off-unit amounts without converting** (no fake lb↔cup math), tiebreaking to a plain count so "2 carrots + 0.5 cup" shows "2", not "0.5 cup". Keyed on the AI's canonicalName, so it only ever *increases* merging for the named set and can **never** merge two different items (a table miss = status quo). +16 aggregate/buy-unit tests. |
| BUG-001 | 🟡 | **Quick-add optimistic category misfired on compound words** — `guessCategory`'s substring `includes` sent "watermelon"→beverages (via "water"), "butternut"→dairy (via "butter"), "eggplant"→dairy (via "egg") for ~1s until the async `tidyItem` re-homed the row. | S28 (2026-07-21), Slice C/D code review (#4) | **S31 (2026-07-22)** | `guessCategory` now matches **whole words** (+ simple -s/-es plural), with an allowlist for intentional stems (`berr` → blueberry/berries) and multiword phrases, plus added produce terms (watermelon/melon/squash/eggplant). "water"↛"watermelon", "butter"↛"butternut", "egg"↛"eggplant"; "blueberries"/"ground beef" still classify right. New `grocery-categories.test.ts`. Note: a pre-existing keyword-ordering quirk (`ice cream`→dairy via "cream", `baking soda`→beverages) is unchanged and self-corrects via the AI tidy. |
| BUG-004 | 🔴 | **Full-week grocery generation is slow / can time out.** A single batched `ingredient-normalize` over a 7-dinner week (~70 lines) took **37.7s** (S28) on the confirm critical path — over the old 30s AI timeout — so a real week's list could fail normalize, and perceived latency was far past Griffin's "even 15s is too long" bar. | S28 (2026-07-21), real-model merge eval | **S30 (2026-07-21)** | Generation-architecture rethink (plan `~/.claude/plans/resume-meal-app-peppy-simon.md`). Normalize moved OFF confirm: each recipe normalizes during plan review (a decoupled best-effort `plan.normalizeSlot` the walker fires after hydrate) and caches on the recipe row (`normalized_ingredients`, migration `0005`); confirm reads the cache and AI-normalizes only cache-misses (zero AI calls on a fully-reviewed week → instant aggregate). Phase D added the honest "Finishing N recipes…" straggler hint + early-confirm instrumentation. **Real-model eval PASSED:** per-recipe normalization == the old batch's merge quality (garlic ×6, salt ×4, olive-oil ×4 identical; scallion↔green-onion synonym canonicalized identically with no co-occurrence advantage), and the ~27–37s batch is entirely off confirm (confirm→ready ≈ aggregate-only, ~0ms). Code review found + FIXED a rate-limit fan-out regression (`bgAiProcedure` — the background normalize gets its own bucket so it can't 429 a user-visible hydrate). 306 unit + 53 E2E green. The S28 60s stopgap timeout stays as a belt-and-braces for the rare residual batch. |
| BUG-R03 | 🟠 | Grocery item **name** was edited via a bare `<p onClick>` — not keyboard/screen-reader reachable (a11y). | S28, code review #1 | S28 (2026-07-21) | Converted to a keyboard-accessible `<button>` with `aria-label`; merge dot marked `aria-hidden`. |
| BUG-R02 | 🟠 | Manual-mode reorder renumbered only **unchecked** items → checked items reappeared out of order once unchecked. | S28, code review #2 | S28 (2026-07-21) | `onItemDragEnd` now renumbers the whole list (reordered visible items + checked items, contiguous 0..N-1). |
| BUG-R01 | 🟡 | `commitEdit` double-fired on Enter (keydown + unmount blur) → a redundant duplicate `editItem` write. | S28, code review #3 | S28 (2026-07-21) | Added a `handledRef` guard; Escape also marks handled so the blur can't commit discarded text. |

---

## How to use this (protocol)

1. **Parking a bug = an Open row here.** Assign the next `BUG-00N`, a severity, a concrete repro, the
   session found, and an honest "address by" target (a phase, a fast-follow, or "next"). Never park a
   defect with only a mention in changelog/backlog.
2. **Every session end:** skim Open. Anything addressed moves to Resolved (keep its ID, add the date +
   how). Anything whose target has arrived gets pulled into that session's plan.
3. **Cross-link:** reference the `BUG-00N` id in code comments where a stopgap or known-issue lives, and
   in the changelog entry for the session that finds or closes it.
