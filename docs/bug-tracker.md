# Bug Tracker — parked defects & risks

> **What this is.** The single home for every **known defect or risk we've chosen to park**
> rather than fix on the spot. A bug does not get "deferred to the backlog" — it gets an entry
> here, with a **repro**, a **severity**, a **when-to-address target**, and a **status we close out**.
> Nothing rots: every session end reviews the Open table, and closing an item moves it to the
> Resolved log with the date + how.
>
> **Distinct from the other docs:** `idea-backlog.md` = features/ideas; `open-questions.md` =
> unresolved *decisions*; **this file = reproducible things that are wrong or risky.** If you can
> write a repro for it, it belongs here.

**Severity:** 🔴 high (breaks a core flow / data / trust) · 🟠 med (degrades UX, has a workaround) ·
🟡 low (cosmetic / self-correcting).
**Status:** `open` · `in-progress` · `resolved` (→ moved to the Resolved log).

---

## Open

| ID | Sev | Summary | Repro | Found | Address by | Status |
|----|-----|---------|-------|-------|-----------|--------|
| BUG-023 | 🟡 | **`plan.modify` reports changed DAYS, not slot ids, so the change ring cannot be truly row-level at multi-meal density.** 1E.5 ledger §C: *"The meal row is the unit of change feedback, never the day container"* — the ring sits on the changed row's own 14px radius inside the day's 18px. The client resolves each changed date to the cookable rows on it (`plan-page-client.tsx`, `idsOnDates`), which at R1's one-dinner-per-day is **exactly** row-level. At the three-meal density the same ledger specifies, a whole-day change would ring dinner, lunch and breakfast together — feedback pointing at two rows the chef did not touch. | Not reachable in production: generation produces dinners only. Reachable the moment lunch/breakfast generation ships, or via a hand-seeded multi-meal day + `plan.modify`. | S43 (2026-07-27), building 1E.5 W1/W3 | **Before multi-meal generation ships** (V1.5+). Fix is server-side: return `changedSlotIds` alongside `changedDates` from `plan.modify`, and have the client prefer it. Cheap — the mutation already knows which slots it wrote. | `open` |
| BUG-022 | 🟡 | **The tab bar ghosts through an open bottom sheet.** `DrawerContent` uses `.glass-sheet`, which is the spec's L5 floating fill at **`.94`** alpha — deliberately translucent — but the tab bar is `position: fixed` at `z-50` and the sheet does not cover it opaquely, so `Plan · Recipes · Groceries · You` reads faintly *through* the sheet's lower third, directly behind the freeform textarea. **This is the same class S40 fixed on the mic toast** ("the mic toast was letting the confirm + skip labels ghost through it at the spec's `.94` L5 fill"), reached by a different surface. **Not a 1E.7 regression** — verified identical in the S41 capture on the old indigo palette (`A-groceries-2026-07-27T03-04-08-001Z/grocery-chef-sheet.png` vs `A-groceries-2026-07-28T01-35-40-074Z/`), so the sweep changed its colour and nothing else. | Groceries → tap the brain icon to open the Talk-to-Chef sheet. The tab labels are legible behind the textarea. Same on Plan's sheets. | S42 (2026-07-27), 1E.7 `/visual-qa` re-capture | **1F, with spec §12 item 04** — that item already deletes the floating search pill and FAB and squares the nav's top corners, so the bottom-edge stack is being reworked there anyway and fixing the ghost in isolation would be work done twice. Options: hide the tab bar while a sheet is open, or give the sheet an opaque backing layer beneath the `.94` glass. Do **not** raise the L5 alpha — `.94` is the spec's value and the mic-toast fix solved its version without touching it. | `open` |
| BUG-011 | 🟠 | **`householdSize` and `householdComposition` can desync, putting two contradictory facts in the same chef prompt.** Composition → size is derived in one place (`deriveHouseholdSize`), but `householdSize` has three independent writers that never touch composition: `user.updatePreferences` (bare scalar still accepted), the You-tab stepper (`field-edit-sheet.tsx`), and the `set_household` talk op (`preferences-talk.ts`). Nothing reconciles back. `getChefContext` then renders both independently, so the prompt can carry `Default servings: 4` alongside "Cooking for 2 adults and 1 baby… under 6 months and not on solids". Found independently by the correctness and architecture reviewers. | Complete onboarding as 2 adults + 1 baby (under 6m) → derived size 2. Then say "we're 4 people now" in Talk-to-the-Chef → `household_composition` is untouched and the next generation prompt states both numbers. | S37 (2026-07-25), `/code-review` correctness + architecture lenses | Pick one direction and enforce it: either the scalar writers adjust `composition.adults` and let the derivation run, or `householdSize` becomes read-only in the input schema. Do it **before V1.5 family profiles** reads composition directly. | `open` |
| BUG-012 | 🟠 | **The You tab renders "4 adults" for a household of 2 adults + 2 children.** `soft-constraints-card.tsx` still prints `householdSize` with an "adults" label, but `householdSize` stopped meaning adults in S36 — it is now a derived servings count folding in children and 12-24mo babies. `describeHousehold(composition)` already produces the right string. Directly undercuts the tab's job: it is the surface where you verify the chef isn't wrong about you. | Onboard with 2 adults + 2 children → You tab → "Cooking for: 4 adults". | S37 (2026-07-25), `/code-review` architecture lens | Next You-tab touch, or with BUG-011 (same root). Cheap. | `open` |
| BUG-010 | 🟠 | **`household_composition` ships with a column DEFAULT, so a default is indistinguishable from an answer.** Migration `0006` gives the column `'{"adults":2,"children":0,"babies":0,"babyStage":null}'`, so every pre-existing row and every row created by an unrelated write carries a composition byte-identical to a real reply. Same class as the `dietary_framework` default that Layer B caught lighting "No restrictions" over an unanswered question (fixed S37). Harmless *today* only by accident — `describeHousehold` happens to return null for adults-only — but `seedChips` already reads `composition.children > 0`, and V1.5 member profiles will read it directly. Contradicts what the rest of the build enforces: `skipCore` deliberately writes nothing, and the skip path synthesizes an honest "Skipped the intro questions" memory rather than a fabricated one. | Any user who never answered the household question has a `household_composition` row claiming 2 adults. | S37 (2026-07-25), `/code-review` architecture lens | Make it nullable with no default (matching `onboarding_completed_at` in the same migration, where NULL correctly means "hasn't run"). `DEFAULT_HOUSEHOLD_COMPOSITION` stays as the client's stepper starting state. Needs a migration. | `open` |
| BUG-013 | 🟠 | **`finishOnboarding` trusts client-supplied memory text.** The mutation calls `synthesizeMemories(input.state)`, and for each deep answer it persists `answer.memory` — a string the **client** computed — bounded only by `z.string().max(300)`. The code's comments describe this as deterministic server-side synthesis from typed answers; it isn't. An authenticated caller hitting the endpoint directly can plant 300 chars into `ai_memories` stamped `sourceType: 'onboarding'`, the provenance the You ledger shows as "You told me when we started". Scoped to the caller's own household, and `chef-system.ts` already treats memories as untrusted user-role content, so this is provenance/data-integrity rather than prompt injection. | Call `trpc.user.finishOnboarding` with a hand-crafted `deepAnswers[].memory`. | S37 (2026-07-25), `/code-review` security lens | Recompute `memory` server-side from `(questionId, dimension, values)` against the server's own question table and ignore the client's field. Before R1 ship. | `open` |
| BUG-020 | 🟠 | **`retryFailed` retries FAILED saves but never awaits IN-FLIGHT ones, so "All saved." can be shown over a save that has not landed yet.** `useCoreSaves.persist` is deliberately fire-and-forget (correct — blocking each turn would put a stutter on every answer), and `retryFailed` returns `true` the moment `failedSaves` is empty. A save still in flight is in neither bucket. Tap through the last core turn onto reflect and hit "Plan my first week" fast enough on a slow connection and `finishOnboarding` succeeds, the reflect screen says "All saved.", and the pending `updatePreferences` fails afterwards with nothing left to retry and no surface to report on. This is the same class BUG-016 closed — the honest-about-saves contract — reached by the one path that fix did not cover. | Throttle the network to 3G, answer the weeknight turn, and tap through the deepen-offer to reflect and straight onto "Plan my first week". | S40 (2026-07-26), 1E-close code review | Track an in-flight count in `useCoreSaves` and have `retryFailed` await it before reporting success. Low probability (several taps sit between the last core save and the CTA) but the interview fires **once per account**, so fix before any second user runs it — i.e. with BUG-011/012 in the next bug-burn session. | `open` |
| BUG-021 | 🟠 | **`skipAll` walks the user out to Plan even when `skipOnboarding` fails**, doing the exact opposite of the fix BUG-016 landed on the finish path. `skipMutation.mutate(undefined, { onSuccess: leaveToPlan, onError: leaveToPlan })` routes both outcomes to the same place, so a failed skip leaves `onboardingCompletedAt` NULL — and the first-run gate drops the user back into the interview on their next load with no explanation of why the skip did not stick. No data is lost (unlike a failed finish), which is why this is 🟠 and not 🔴, but "Skip for now" silently not sticking is a trust bug on a first run. | Abort `user.skipOnboarding`, tap "Skip for now" → lands on Plan; reload → back at `/welcome`. | S40 (2026-07-26), 1E-close code review | Mirror the finish path: stay put and toast a retry rather than leaving. Same session as BUG-020. | `open` |
| BUG-019 | 🟡 | **GR7 (grocery section drag-reorder) is flaky in a full-suite run.** Failed once in a full `npm run test:e2e` on S38, then passed in isolation and passed again with the whole `groceries.spec.ts` file. Nothing in that session touched Groceries. Pointer-driven `@dnd-kit` drags are timing-sensitive, and a flaky spec is worse than a missing one: it trains us to re-run instead of read. | `npm run test:e2e` (full suite, sequential) → occasionally fails GR7; `-g "GR7"` alone passes. | S38 (2026-07-25), full-suite run during the 1E close. **RECURRENCE #2: S44 (2026-07-28)** — failed in the final full-suite run and passed in isolation immediately after. Nothing in S43 or S44 touched Groceries, and every other GR spec was green in the same run, so this is the flake and not a regression. | Stabilize the drag (explicit pointer-move steps / wait for the dnd overlay) rather than retrying. **One more recurrence and it gets quarantined** — the tracker's own threshold, now one away. The cost of leaving it is that it trains us to re-run instead of read, which is exactly how a real Groceries regression would slip past. Worth doing in the next Groceries touch or alongside Slice 2's Recipes bottom-edge work. | `open` |
| BUG-017 | 🟡 | **The first-run gate is not synchronous with first paint.** `OnboardGuard` fires `ensureOnboarded` from an effect while the destination route renders and fires its own queries in parallel. On a genuinely first-ever login `ensureOnboarded` is the only thing that inserts the `household_members` row, and `protectedProcedure` throws FORBIDDEN without it — so the page's own queries can race the bootstrap and fail before the redirect lands. React Query's default retry papers over it incidentally, not by design. | Brand-new account, first ever load of `/plan`. Hard to reproduce reliably; read the code path. | S37 (2026-07-25), `/code-review` perf/UX lens | 1F hardening, unless it shows up in Griffin's or his wife's real first run — in which case it is the first thing to fix. | `open` |
| BUG-018 | 🟠 | **The E2E harness deletes rows in the real Supabase project, guarded only by a household name.** `seed.ts` runs deletes over a service-role-equivalent direct Postgres connection against production, and the sole barrier is `assertTestHousehold()` checking that the household's `name` equals `"E2E Test Kitchen"` with one member. There is no environment check on `DATABASE_URL` and no DB-level privilege limit. Low impact today (one real user), but a misconfigured `.env.local` on a second machine — or a real household ever named that — is a data-loss event, and this session already saw the harness's auth user vanish mid-run during a concurrent session. | Point `DATABASE_URL` at any project with a household named "E2E Test Kitchen" and run the suite. | S37 (2026-07-25), `/code-review` security lens | Add an `assertNotProductionUrl()` allow-list check alongside the name check; a separate non-prod Supabase project is the real answer. Before any second machine runs the suite. | `open` |
| BUG-003 | 🟡 | **Cooked-harvest writes inside a `recipe.list` query.** `recipe.list` (a tRPC query / GET-shaped) runs `harvestCookedRecipes` — a grouped select + per-recipe UPDATEs — on every load and every favorite invalidation. Idempotent & documented, but write-work on a read path that React Query may refetch/retry. | N/A (design smell, not a user-visible fault today). | S28 (2026-07-21), Slice C/D code review (finding #5) | Revisit if/when a scheduler exists (move the harvest to a cron/confirm-time job) or if `recipe.list` perf degrades. Accepted for V1. | `open` |

---

## Resolved

| ID | Sev | Summary | Found | Resolved | How |
|----|-----|---------|-------|----------|-----|
| BUG-024 | 🟠 | **The Plan E2E specs selected against a DOM the 1E.5 rail replaced — 21 of 78 red.** Spec migration owed by an intentional rebuild, not a behaviour regression, but it left the Plan tab with **no mechanical gate**. | S43 (2026-07-27) | S44 (2026-07-28) | Migrated, not weakened — and the tracked cause list was **incomplete**. Three causes were logged; **five** were real. (1) `reviewHero`'s heading → the `data-testid="plan-rail"` anchor. (2) chips moved to the meal sheet → `runMealAction` opens the sheet first. (3) the in-card `Reworking …` label → the gold ring, asserted via **computed `box-shadow`** rather than a class name, because §C's rule is that the ring is *gold* and a class match would pass on a grey one. **(4, untracked) the standalone `Talk to the Chef` button is gone** — the whole-week door is the chef header's `Something's off` — which took M4/M5/M7. **(5, untracked) `Start over →` vanished from the draft screen entirely** (see BUG-026). Assertions came out **stronger**: M1 now proves the ring is on the row and nowhere else; M5 measures the toast against the confirm bar's real bounding box; M6 proves every chef action goes inert rather than that one tap was silently dropped. |
| BUG-006 | 🟡 | **Plan's expanded sheet showed the FULL recipe, not a summary** — `ExpandedMealSheet` rendered the whole `RecipeView` (ingredients + steps) inline, making Plan a second, worse Recipes tab. | S35 (2026-07-24), Griffin testing | S44 (2026-07-28), 1E.5 W7 | Rebuilt to the wave-1 settled structure: identity (day · meal · meta · title · gold rationale at feature size), then **one action model, two kinds of action** — *chips ask the chef, rows go somewhere* — grouped `ASK ME FOR A CHANGE` then `TAKE IT SOMEWHERE`. **Ingredients and steps never enter Plan**; one row leaves for Recipes and it **states its own state** (a recipe still being written is not a link, a failed one says so) rather than offering a door that goes nowhere. Covered by **P8**. Worth knowing: this is a real subtraction — reading the full recipe while reviewing the week used to happen here, and the ledger's trade is that it is one tap away in the tab that owns it. |
| BUG-008 | 🟡 | **The Plan meal card printed the cook time twice**, and two *different* times when `estTimeMinutes` and a time-shaped tag disagreed ("95 min · … · 90 min"). | S37 (2026-07-25), ADVERSARIAL Layer-A capture | S44 (2026-07-28), 1E.5 W1 | Broader than tracked: the meta row **had no contract at all** — it appended `estTimeMinutes`, then servings, then every tag verbatim. `rail-helpers.metaLine` now emits one cook time and one serving count; a time-shaped tag is **dropped rather than deduped**, because a card can state one cook time honestly and `estTimeMinutes` is the structured one. Non-time tags become markers above the title, where they can never read as a second duration. Unit-locked in `rail-helpers.test.ts` (both the disagreeing and agreeing cases) and now **gated by P3** against the ADVERSARIAL seed. |
| BUG-009 | 🟡 | **A slot with no title rendered as a permanent "Thinking…" card** — a settled plan presenting a forever-loading state. | S37 (2026-07-25), ADVERSARIAL Layer-A capture | S44 (2026-07-28), 1E.5 W5 | A titleless cookable slot is now the **provisional row**: the spec's hueless `.09` fill with a `.2` line, a muted sentence where the title goes, **never a spinner**. The same object serves "not written yet" and "deliberately undecided" because the user-facing fact is identical — nothing is planned here and nothing was bought for it — which is also why generation needs no separate streaming vocabulary. It holds its height, so resolving changes text and fill only and nothing reflows. **Confirm stays live over it**, and the count says four dinners, not five, so a hole can never be mistaken for something the shop covers. Gated by **P4**. |
| BUG-025 | 🟠 | **The rail's floating slot was `absolute`, so it scrolled away with the content.** The frame's "bottom 96" is a distance from the screen edge; on a phone canvas those are the same thing, but the app's scroll container is the document, so `absolute bottom-24` anchored to the bottom of the *content*. On a seven-day draft, `Confirm 7 dinners` was only reachable by scrolling to the very end — and §D calls for exactly one floating layer above the tab bar, which a layer you have to hunt for is not. The retired `BottomBar` had used `fixed` and was viewport-anchored. | S44 (2026-07-28), migrating BUG-024 — surfaced as Playwright "element intercepts pointer events" on RG1 | S44 (2026-07-28) | `SLOT` is now `fixed`, using the tab bar's own centring trick (`fixed` positions against the viewport, which costs the column its `mx-auto`). Geometry is unchanged: bottom 96, height 52, radius 16, insets 16. **M5 now measures it** — it takes the confirm bar's bounding box, fires a modify, and asserts the toast matches on all four dimensions. |
| BUG-026 | 🟠 | **The rebuild dropped the regenerate airlock's draft door.** The chef header carries ONE revise control and frame `3i` spends it on `Something's off` (→ Talk to the Chef), so on a draft there was **no way to re-prompt at all**. The brief's state inventory §3 explicitly keeps that door ("Start over" on a draft, "Plan a new week" on a confirmed week), and scope-1E.5's own acceptance criteria require RG1–RG5 not to regress. A feature loss, not a spec problem. | S44 (2026-07-28), migrating BUG-024 — RG1/RG3/RG4/RG5 | S44 (2026-07-28) | Restored as the **foot link under the rail**, which is the pattern `plan-midweek.tsx` already uses for the same job — so it adds no new object and does not breach §D's one-floating-layer rule. Draft only; on a confirmed week the header's control *is* `Plan a new week`. **Griffin's call is open on placement**: frame `3i` does not draw the bottom of the scroll, so this is a reading of the ledger rather than a locked decision. |
| BUG-027 | 🟡 | **§D's scroll padding (168px draft / 108px confirmed) was never built**, so nothing below the rail could clear the floating primary. Compounded BUG-025. | S44 (2026-07-28) | S44 (2026-07-28) | `SLOT_PADDING_DRAFT` / `SLOT_PADDING_BARE`, named beside the slot they exist for and applied by `PlanReview`, `PlanMidweek`, `StreamingPlan` and `WeekWrappedState`. |
| BUG-028 | 🟠 | **The Plan tab had no heading at all.** Deleting the "Your week, ready to review" hero took the surface's only `<h2>` with it — the chef's claim replaced it as a `<p>`, so assistive tech had no landmark on the app's signature screen. | S44 (2026-07-28), rebuilding week-wrapped | S44 (2026-07-28) | `ChefHeader`'s summary is an `<h2>`. The type is unchanged; only the element is. It is the screen's claim on every Plan state, so this restores the landmark everywhere rather than patching one screen. |
| BUG-029 | 🟠 | **A failed modify became unreachable once the user walked away from the sheet.** The rail moved every modify into a sheet, a failing sheet deliberately stays open, `ModifyStatusPills` was gated on `!sheetOpen`, and dismissing the sheet called `clearError()` on the way out — so the scroll-independent retry (X1's whole mechanic) could never render. | S44 (2026-07-28), migrating BUG-024 — fixme'd rather than weakened, then fixed | S44 (2026-07-28), 1E.5 W3 | The error lives in the **action bar's slot**, which does not care whether a sheet is open, and dismissing a sheet no longer clears it (a fresh action still does). X1 is now **stronger** than the test it replaced: it asserts the failure *survives* the dismissal, which is the part that was broken. |
| BUG-016 | 🔴 | **A failed preference save during onboarding was silent, and the interview still said "All saved."** The four core turns fired `savePreferences.mutate(...)` with no `onError`, and `finishOnboarding` routed both outcomes to the same place. A dropped connection lost that answer — or every synthesized memory — with no indication and no retry, on a flow that fires exactly once per account. | S37 (2026-07-25), `/code-review` perf/UX lens | S39 (2026-07-25) | New `use-onboarding-saves.ts` (`useCoreSaves`) owns core persistence: a failed save is **named** in a toast, **remembered** keyed by the answer's label, and **retried** when the user taps "Plan my first week" — the natural end point, by which a transient drop has usually healed. The reflect screen no longer claims "All saved" while anything is outstanding (`onboarding-unsaved-note`). A failed `finishOnboarding` now **stays on reflect** with a live retry instead of walking the user out to a plan built on nothing — that path saved neither the memories nor the completed flag, so leaving silently just deferred the loss to the next load. Deliberately does NOT block each turn on its round trip (that trades a rare failure for a stutter on every answer). Regression-guarded by **OB15**, which aborts the first `user.updatePreferences` for real and asserts the dropped answer reaches the database. |
| BUG-015 | 🟠 | **Nothing disabled the tap controls while a free-text capture was in flight, so two writes raced.** Type "we're vegan", tap Pescatarian, confirm before the reply lands: two mutations hit the same field and the winner was whichever request finished last. | S37 (2026-07-25), `/code-review` perf/UX lens | S39 (2026-07-26) | The confirm on both the shared question screen and the household screen is now `disabled={talkPending}`. Disabled rather than hidden: the button was already on screen, and removing it from under a thumb is worse than dimming it for the second the reply takes. Chip taps stay live, since only the confirm writes. |
| BUG-014 | 🟠 | **Typed free text was cleared before the request resolved, so a failed capture lost what the user wrote** — while the toast invited them to try again. | S37 (2026-07-25), `/code-review` perf/UX lens | S39 (2026-07-25) | `submitFreeText` now resolves a boolean (`mutateAsync` in place of fire-and-forget `mutate`) and `TellMeField` clears **on success only** — the same rule `shared/talk-to-chef-sheet.tsx` already followed. A committed write whose read-back fails still counts as success, since the message did land. Regression-guarded by **OB14** (aborts `user.talk`, asserts the field still holds the text). |
| BUG-005 | 🟠 | **App-wide serif: `font-sans` never resolved to Geist.** Every heading and every line of prose on every tab rendered in the browser's default serif. | S33 (2026-07-22), You visual-QA — parked pending an on-device check, on the theory it might be capture-only | S37 (2026-07-25) | **Not capture-only, and no device check was needed.** `globals.css` had `--font-sans: var(--font-sans)` — a self-reference, invalid at computed-value time, so the token could never resolve anywhere; Geist was loaded in `layout.tsx` as `--font-geist-sans` with nothing consuming it. One-line bridge. Deliberately fixed inside 1E rather than deferred to the 1F design-system pass (the tracker's original disposition), because Griffin's taste pass on the onboarding copy was about to happen in the wrong typeface. Blast radius checked: Layer-A re-capture of Plan, Groceries, Recipes and You — no truncation or reflow damage on any surface. |
| BUG-007 | 🔴 | **E2E harness couldn't authenticate — `bad_jwt` on every Supabase admin call.** All 70 specs failed to run. | **Root cause was the harness, not the credentials.** The project uses the new `sb_publishable_`/`sb_secret_` key format with asymmetric (ES256) JWTs. Those keys are valid — PostgREST accepts the secret key, and ordinary password sign-in with the publishable key works — but GoTrue's `/auth/v1/admin/*` endpoints still expect a JWT carrying `role: service_role`, and `sb_secret_…` isn't a JWT, so they reject it. `mintSupabaseSession` was calling `auth.admin.listUsers` + `updateUserById` to bootstrap a user that **already existed**. Fixed by signing in with the publishable key first (which also drops a 50-page `listUsers` scan from every run) and falling back to the privileged bootstrap only when sign-in genuinely fails. No key change needed; **Griffin's `.env.local` was already correct.** | S36 (2026-07-25) — 70/70 E2E green after the fix |
| BUG-002 | 🟠 | **Merge produced duplicate name-lines** — the same canonical item on two rows: `Salt 3.25 tsp` + `Salt (to taste)`, `Black pepper 2.25 tsp` + `Black pepper`, `Carrot 1.5 lb` + `Carrot 0.5 cup`. Safe under-merge, but untidy to a shopper. | S28 (2026-07-21), real-model merge eval | **S31 (2026-07-22)** | Added a **buy-unit table** to the aggregator (`src/server/grocery/buy-units.ts`). Items a shopper buys as ONE thing consolidate to a single row: **staples** (salt, pepper, oils, dried spices, vinegars) collapse to one **unquantified** row — the measured tsp is shopping noise (Griffin's call: drop the number, the per-meal amounts survive in the amber-dot breakdown); **concrete buy-unit** produce (carrot→lb, onion→count, …) collapses to one row that sums the buy-unit and **absorbs off-unit amounts without converting** (no fake lb↔cup math), tiebreaking to a plain count so "2 carrots + 0.5 cup" shows "2", not "0.5 cup". Keyed on the AI's canonicalName, so it only ever *increases* merging for the named set and can **never** merge two different items (a table miss = status quo). +16 aggregate/buy-unit tests. |
| BUG-001 | 🟡 | **Quick-add optimistic category misfired on compound words** — `guessCategory`'s substring `includes` sent "watermelon"→beverages (via "water"), "butternut"→dairy (via "butter"), "eggplant"→dairy (via "egg") for ~1s until the async `tidyItem` re-homed the row. | S28 (2026-07-21), Slice C/D code review (#4) | **S31 (2026-07-22)** | `guessCategory` now matches **whole words** (+ simple -s/-es plural), with an allowlist for intentional stems (`berr` → blueberry/berries) and multiword phrases, plus added produce terms (watermelon/melon/squash/eggplant). "water"↛"watermelon", "butter"↛"butternut", "egg"↛"eggplant"; "blueberries"/"ground beef" still classify right. New `grocery-categories.test.ts`. Note: a pre-existing keyword-ordering quirk (`ice cream`→dairy via "cream", `baking soda`→beverages) is unchanged and self-corrects via the AI tidy. |
| BUG-004 | 🔴 | **Full-week grocery generation is slow / can time out.** A single batched `ingredient-normalize` over a 7-dinner week (~70 lines) took **37.7s** (S28) on the confirm critical path — over the old 30s AI timeout — so a real week's list could fail normalize, and perceived latency was far past Griffin's "even 15s is too long" bar. | S28 (2026-07-21), real-model merge eval | **S30 (2026-07-21)** | Generation-architecture rethink (plan `~/.claude/plans/resume-meal-app-peppy-simon.md`). Normalize moved OFF confirm: each recipe normalizes during plan review (a decoupled best-effort `plan.normalizeSlot` the walker fires after hydrate) and caches on the recipe row (`normalized_ingredients`, migration `0005`); confirm reads the cache and AI-normalizes only cache-misses (zero AI calls on a fully-reviewed week → instant aggregate). Phase D added the honest "Finishing N recipes…" straggler hint + early-confirm instrumentation. **Real-model eval PASSED:** per-recipe normalization == the old batch's merge quality (garlic ×6, salt ×4, olive-oil ×4 identical; scallion↔green-onion synonym canonicalized identically with no co-occurrence advantage), and the ~27–37s batch is entirely off confirm (confirm→ready ≈ aggregate-only, ~0ms). Code review found + FIXED a rate-limit fan-out regression (`bgAiProcedure` — the background normalize gets its own bucket so it can't 429 a user-visible hydrate). 306 unit + 53 E2E green. The S28 60s stopgap timeout stays as a belt-and-braces for the rare residual batch. |
| BUG-R03 | 🟠 | Grocery item **name** was edited via a bare `<p onClick>` — not keyboard/screen-reader reachable (a11y). | S28, code review #1 | S28 (2026-07-21) | Converted to a keyboard-accessible `<button>` with `aria-label`; merge dot marked `aria-hidden`. |
| BUG-R02 | 🟠 | Manual-mode reorder renumbered only **unchecked** items → checked items reappeared out of order once unchecked. | S28, code review #2 | S28 (2026-07-21) | `onItemDragEnd` now renumbers the whole list (reordered visible items + checked items, contiguous 0..N-1). |
| BUG-R01 | 🟡 | `commitEdit` double-fired on Enter (keydown + unmount blur) → a redundant duplicate `editItem` write. | S28, code review #3 | S28 (2026-07-21) | Added a `handledRef` guard; Escape also marks handled so the blur can't commit discarded text. |

---

## How to use this (protocol)

1. **Parking a bug = an Open row here.** Assign the next `BUG-00N`, a severity, a concrete repro, the
   session found, and an honest "address by" target (a phase, a fast-follow, or "next"). Never park a
   defect with only a mention in changelog/backlog.
2. **Every session end:** skim Open. Anything addressed moves to Resolved (keep its ID, add the date +
   how). Anything whose target has arrived gets pulled into that session's plan.
3. **Cross-link:** reference the `BUG-00N` id in code comments where a stopgap or known-issue lives, and
   in the changelog entry for the session that finds or closes it.
